// Optional config file. Edit and keep with the site files.
// If present, index.js will use this instead of petmon.ini.
// Example:
// window.PETMON_CONFIG = { deviceCode: 'SW0001', branchName: '홍대점' };

window.PETMON_CONFIG = window.PETMON_CONFIG || null;
